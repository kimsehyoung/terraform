apiVersion: karpenter.k8s.aws/v1beta1
kind: EC2NodeClass
metadata:
  name: ${name}
spec:
  amiFamily: ${ami_family}
  role: ${role}
  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: ${selector_tag}
  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: ${selector_tag}
  tags:
    karpenter.sh/discovery: ${selector_tag}